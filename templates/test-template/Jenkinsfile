pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                def devData = readYaml file: 'https://github.com/anurodhmohapatra/JenkinsTest/blob/main/templates/test-template/template.yaml' 
                def serviceAccountName = devData.service-account-name
                echo "Building.. ${serviceAccountName}"
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
